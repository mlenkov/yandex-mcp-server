# --- Database (Dual DB) ---
# Для разработки (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./data/yandex-mcp.db
# Для продакшена (MySQL 8):
# DATABASE_URL=mysql+aiomysql://user:password@host:3306/yandex_mcp

# --- Encryption ---
# Сгенерировать: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# --- Yandex OAuth ---
YANDEX_CLIENT_ID=
YANDEX_CLIENT_SECRET=
YANDEX_REDIRECT_URI=https://app.mais.agency/oauth/callback

# --- Server ---
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

from mcp.server.fastmcp import FastMCP


def register_prompts(mcp: FastMCP) -> None:

    @mcp.prompt()
    def analyze_campaign_performance(campaign_id: int, days: int = 7) -> str:
        """Анализ эффективности рекламной кампании за последние N дней."""
        return f"""Проанализируй эффективность кампании {campaign_id} за последние {days} дней.

ШАГИ:
1. Получи контекст аккаунта через get_account_context
2. Получи статистику кампании за период (используй get_direct_stats)
3. Получи статистику за предыдущий аналогичный период для сравнения
4. Рассчитай ключевые метрики: CTR, CPC, CR, CPA, ROI
5. Выяви аномалии (резкие падения/росты)
6. Предложи конкретные оптимизации

ФОРМАТ ОТВЕТА:
- Краткое резюме (1-2 предложения)
- Ключевые метрики (таблица)
- Сравнение с предыдущим периодом
- Выявленные проблемы
- Рекомендации (приоритизированные)
"""

    @mcp.prompt()
    def suggest_budget_optimization(account_id: int | None = None) -> str:
        """Предложения по оптимизации бюджета рекламных кампаний."""
        return """Проанализируй все кампании аккаунта и предложи оптимизацию бюджета.

ШАГИ:
1. Получи список всех кампаний через get_direct_campaigns
2. Для каждой кампании получи статистику за последние 14 дней
3. Рассчитай ROI для каждой кампании
4. Выяви:
   - Кампании с высоким ROI (увеличить бюджет)
   - Кампании с низким ROI (уменьшить бюджет или остановить)
   - Кампании без конверсий (проверить настройки)
5. Предложи конкретное перераспределение бюджета

ФОРМАТ ОТВЕТА:
- Общая статистика аккаунта
- Топ-5 лучших кампаний (увеличить бюджет на X%)
- Топ-5 худших кампаний (уменьшить бюджет на X% или остановить)
- Ожидаемый эффект (рост ROI на X%)
"""

    @mcp.prompt()
    def find_underperforming_ads(account_id: int | None = None) -> str:
        """Поиск неэффективных объявлений для оптимизации."""
        return """Найди неэффективные объявления в аккаунте.

ШАГИ:
1. Получи список кампаний
2. Для каждой кампании получи объявления
3. Получи статистику по каждому объявлению (CTR, CPC, конверсии)
4. Выяви объявления с:
   - CTR < 1% (низкая релевантность)
   - CPC > среднего по кампании (дорогие)
   - 0 конверсий при >100 кликах (не работают)
5. Предложи улучшения (новые заголовки, тексты, ключевые слова)

ФОРМАТ ОТВЕТА:
- Топ-10 худших объявлений (с метриками)
- Причины низкой эффективности
- Конкретные предложения по улучшению
"""
